var host = {};

/**
 * @param {String} projectUrl 프로젝트 경로
 * @param {String} directoryUrl 디렉토리 경로
 * @param {String} fileUrl 파일 경로
 * @param {(data:host_callback)=>void} callback 콜백함수 
 * 
 */

// 파일 경로를 반환


module.exports = host;